using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Arrow_Wall : MonoBehaviour
{
   /* public GameObject arrowPrefab; // 화살 프리팹
    public float spawnInterval = 1.0f; // 화살 생성 간격

    IEnumerator Start()
    {
        while (true)
        {
            SpawnArrow(); // 화살 생성
            yield return new WaitForSeconds(spawnInterval); // 일정 시간 대기 후 다음 화살 생성
        }
    }

    void SpawnArrow()
    {
        GameObject arrow = Instantiate(arrowPrefab, transform.position, Quaternion.identity); // 화살 생성
        // 화살이 향하게 할 방향 설정 (예: 벽 오브젝트가 어떤 방향을 향하고 있으면 그 방향으로 설정)
    }*/
}